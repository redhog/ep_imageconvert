A back-end plugin building on top of the file upload plugin to enable server side conversion, image scaling and cropping.

# Installation

    apt-get install imagemagick poppler-utils ghostscript sed
